<!DOCTYPE html>
<html >
  <head>
    <meta charset="UTF-8">
    <title>Login/Logout animation concept</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=yes">

    <link rel='stylesheet prefetch' href='https://fonts.googleapis.com/css?family=Open+Sans'>

    <link rel="stylesheet" href="/stylesheets/style.scss">

    <link href="<%= image_path("logo.png") %>" rel="shortcut icon" />

  </head>
  <body>
    <div class="cont">
      <div class="demo">
        <div class="login">
          <div class="login__form">
            <div class="login_check">
              <img src="<%= image_path("logo.png") %>" style="width:200px; height:200px;">
            </div>
            <div class="login__row">
              <h2>Smessager</h2>
                  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                    <div class="field">
                      <div class="login__row">
                        <svg class="login__icon name svg-icon" viewBox="0 0 20 20">
                        <path d="M0,20 a10,8 0 0,1 20,0z M10,0 a4,4 0 0,1 0,8 a4,4 0 0,1 0,-8" fill="white" />
                      </svg>
                        <%= f.email_field :email, autofocus: true, placeholder: "email@gmail.com" %>
                      </div>
                    </div>

                    <div class="field">
                      <svg class="login__icon pass svg-icon" viewBox="0 0 20 20">
                        <path d="M0,20 20,20 20,8 0,8z M10,13 10,16z M4,8 a6,8 0 0,1 12,0" fill="white" />
                      </svg>
                      <%= f.password_field :password, autocomplete: "off", placeholder: "password" %>
                    </div>

                    <% if devise_mapping.rememberable? -%>
                      <div class="field">
                        <%= f.check_box :remember_me %>
                        <label>Remember me </label>
                      </div>
                    <% end -%>

                    <div class="actions">
                      <%= f.submit "Log in" %>
                    </div>
              <% end %>
              <%= render "devise/shared/links" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
